name = "yujingshistudio"
compatibility_date = "2026-04-25"

# Main site Worker build: serve the built portfolio from ./dist
# (this should match your existing production setup).
[assets]
directory = "./dist"
not_found_handling = "single-page-application"

